# nader_portfolio

Every working rule for this repository lives in `AGENTS.md`, imported below. Read it before
changing anything — the static-export constraint and the confidentiality rules in particular
are hard requirements, not preferences.

`README.md` covers setup and how to edit content. `docs/SPEC.md` is the source of truth for
intent. `docs/DEPLOY.md` covers hosting.

@AGENTS.md
